apiVersion: v1
kind: ConfigMap
metadata:
  name: gitops-repo-nginx
data:
  default.conf: |
    server {
      listen 8080;
      server_name _;
      root /usr/share/nginx/html;

      location / {
        autoindex on;
      }
    }

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: prometheus
  namespace: argocd
  labels:
    app.kubernetes.io/name: prometheus
spec:
  project: default
  source:
    repoURL: git://gitops-git.argocd.svc.cluster.local/gitops.git
    targetRevision: main
    path: apps/prometheus/overlays/production
  destination:
    server: https://kubernetes.default.svc
    namespace: prometheus
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

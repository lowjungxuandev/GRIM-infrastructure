apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana
  namespace: argocd
  labels:
    app.kubernetes.io/name: grafana
spec:
  project: default
  source:
    repoURL: git://gitops-git.argocd.svc.cluster.local/gitops.git
    targetRevision: main
    path: apps/grafana/overlays/production
  destination:
    server: https://kubernetes.default.svc
    namespace: grafana
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
